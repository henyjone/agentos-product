# agent_engine

HGZ / Dev2 owned Python module for AgentOS AI agent orchestration.

This module owns:

- agent mode routing
- structured agent responses
- risk classification before actions
- approval action generation
- memory access abstraction
- lightweight orchestration glue

This module does not own:

- database schema
- backend approval state machine
- external connector sync jobs
- frontend rendering
- knowledge index implementation

